import React from "react";
import "../Components/TaskRemaining.css"

const TaskRemaining = (props) => {
    return (
        <div>
            <h3 className="taskremainingbox">3 tasks remaining </h3>
        </div>
    )
} ;

export default TaskRemaining;